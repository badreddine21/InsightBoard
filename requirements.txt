# --- Web Framework & Architecture (Chapter 2.1 & 3.1) ---
django==5.0.1           # The Core MVC Framework 
gunicorn==21.2.0        # Production Server (for later deployment)
python-dotenv==1.0.0    # For managing security keys

# --- Data Processing & Wrangling (Chapter 3.2 & REQ-02) ---
pandas==2.2.0           # Data cleaning and transformation 
numpy==1.26.3           # Numerical operations
openpyxl==3.1.2         # Engine to read modern Excel (.xlsx) files 
xlrd==2.0.1             # Engine for older Excel (.xls) files

# --- Machine Learning & Analytics (Chapter 3.3 & REQ-04) ---
scikit-learn==1.4.0     # The explainable ML algorithms (K-Means, Regression) 
scipy==1.12.0           # Scientific computing helper for ML

# --- Visualization & Reporting (Chapter 3.4 & REQ-03) ---
plotly==5.18.0          # Interactive charting (Zooming, filtering) 
django-plotly-dash==2.3 # Integration of Plotly into Django (Optional, allows Dash apps inside Django)

# --- Utilities & UI ---
django-crispy-forms==2.1  # Makes the upload forms look professional
crispy-bootstrap5==2024.2 # Bootstrap 5 styling for forms
Pillow==10.2.0            # Image handling (for user avatars)